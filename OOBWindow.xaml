<Window x:Class="ScenarioEditor.OOBWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:SOW="clr-namespace:NorbSoftDev.SOW;assembly=NorbSoftDev.SOW"
    xmlns:local="clr-namespace:ScenarioEditor"
    Title="Master Order of Battle" Height="600" Width="900"
    Loaded="OOBWindow_Loaded">

    <Window.Resources>
        <local:RankToBrushConverter x:Key="RankToBrushConverter"/>
        <local:IdToBrushConverter x:Key="IdToBrushConverter"/>

        <Style x:Key="styleId" TargetType="{x:Type DataGridCell}">
            <Setter Property="Control.Background" Value="LightBlue"/>
            <Setter Property="Control.Height" Value="20"/>
        </Style>

        <Style x:Key="styleUnitCell" TargetType="{x:Type DataGridCell}">
            <Setter Property="Control.Background" Value="White"/>
            <Setter Property="Control.Height" Value="20"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderBrush" Value="Blue"/>
                    <Setter Property="Background" Value="AntiqueWhite"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="styleEchelonRankShaded" TargetType="{x:Type TextBlock}">
            <Setter Property="Background" Value="{Binding echelon, Converter={StaticResource RankToBrushConverter}}"/>
        </Style>

        <Style x:Key="styleNameField" TargetType="TextBox">
            <Setter Property="Control.MinWidth" Value="80"/>
            <Setter Property="Control.Height" Value="20"/>
            <Setter Property="Control.Background" Value="LightBlue"/>
        </Style>

        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True"/>
        </Style>

        <HierarchicalDataTemplate x:Key="EchelonTemplate" ItemsSource="{Binding treeItems}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding symbol}" HorizontalAlignment="Right"/>
                <TextBlock HorizontalAlignment="Left" Margin="5,1,5,1" MinWidth="200">
                    <TextBlock.Text><Binding Path="niceName"/></TextBlock.Text>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding isAlternate}" Value="True">
                                    <Setter Property="FontStyle" Value="Italic"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Foreground="DarkGreen"    Text="{Binding nInfantry}"  HorizontalAlignment="Right" TextAlignment="Right" Margin="5,1,5,1"/>
                <TextBlock Foreground="Yellow"       Text="{Binding nCavalry}"   HorizontalAlignment="Right" TextAlignment="Right" Margin="5,1,5,1"/>
                <TextBlock Foreground="SaddleBrown"  Text="{Binding nArtillery}" HorizontalAlignment="Right" TextAlignment="Right" Margin="5,1,5,1"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <DataTemplate x:Key="FormationTemplate">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <Button Name="FormationButton" Content="{Binding formation.id}" Click="HasFormationButtonClick"/>
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="sRankTemplate">
            <TextBlock Text="{Binding echelon.symbol, Mode=OneWay}"
                       Style="{StaticResource styleEchelonRankShaded}"
                       FontFamily="Courier New" HorizontalAlignment="Right" TextAlignment="Right"/>
        </DataTemplate>

        <local:AddChilOOBEchelonCommand x:Key="AddChilOOBEchelon"/>
        <local:DeleteOOBEchelonCommand x:Key="DeleteOOBEchelon"/>
        <local:AddToScenarioCommand x:Key="AddToScenario"/>
        <local:AddToScenarioWithChildrenCommand x:Key="AddToScenarioWithChildren"/>
    </Window.Resources>

    <DockPanel Background="SlateGray">

        <!-- Menu -->
        <Menu DockPanel.Dock="Top" Background="DimGray">
            <MenuItem Header="Load Order Of Battle..." Click="loadOrderOfBattleClick"/>
        </Menu>

        <!-- OOB name + collapse level buttons -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Height="30" Background="DimGray">
            <Label Content="OOB:" Foreground="White" VerticalAlignment="Center" Padding="4,0"/>
            <TextBox Text="{Binding orderOfBattle.name, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" MinWidth="120"
                     Style="{StaticResource styleNameField}"/>
            <Button Content="XXXX" ToolTip="Army"     MinWidth="34" Margin="4,3,2,3" Click="otv_CollapseToArmy"/>
            <Button Content="XXX"  ToolTip="Corps"    MinWidth="34" Margin="2,3,2,3" Click="otv_CollapseToCorps"/>
            <Button Content="XX"   ToolTip="Division" MinWidth="34" Margin="2,3,2,3" Click="otv_CollapseToDivision"/>
            <Button Content="X"    ToolTip="Brigade"  MinWidth="34" Margin="2,3,2,3" Click="otv_CollapseToBrigade"/>
            <Button Content="III"  ToolTip="Regiment" MinWidth="34" Margin="2,3,2,3" Click="otv_CollapseToRegiment"/>
        </StackPanel>

        <!-- Tree + DataGrid -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <TreeView x:Name="orderOfBattleTree" Grid.Column="0"
                      Background="DarkGray"
                      ItemsSource="{Binding orderOfBattle.root}"
                      AllowDrop="True"
                      ItemTemplate="{StaticResource EchelonTemplate}"
                      SelectedItemChanged="otv_SelectedItemChanged"
                      ContextMenuOpening="otv_ContextMenuOpening"
                      ContextMenuClosing="otv_ContextMenuClosing"
                      DragOver="otv_DragOver"
                      DragEnter="otv_DragOver">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Header="Add to Scenario" Command="{StaticResource AddToScenario}" CommandParameter="{Binding}"/>
                                    <MenuItem Header="Add to Scenario with Subordinates" Command="{StaticResource AddToScenarioWithChildren}" CommandParameter="{Binding}"/>
                                    <Separator/>
                                    <MenuItem Header="Add Child" Command="{StaticResource AddChilOOBEchelon}" CommandParameter="{Binding}"/>
                                    <MenuItem Header="Remove This and All Children" Command="{StaticResource DeleteOOBEchelon}" CommandParameter="{Binding}"/>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="IsExpanded" Value="True"/>
                        <Setter Property="Background" Value="{Binding unit, Converter={StaticResource RankToBrushConverter}}"/>
                        <EventSetter Event="TreeViewItem.DragOver"                  Handler="otv_DragOver"/>
                        <EventSetter Event="TreeViewItem.DragEnter"                 Handler="otv_DragOver"/>
                        <EventSetter Event="TreeViewItem.Drop"                      Handler="otv_Drop"/>
                        <EventSetter Event="TreeViewItem.MouseMove"                  Handler="otv_MouseMove"/>
                        <EventSetter Event="TreeViewItem.PreviewMouseLeftButtonDown"  Handler="otv_MouseLeftButtonDown"/>
                        <EventSetter Event="TreeViewItem.PreviewMouseRightButtonDown" Handler="otv_MouseRightButtonDown"/>
                        <EventSetter Event="TreeViewItem.MouseDoubleClick"            Handler="otv_MouseDoubleClick"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#FFBCBCBC"/>

            <DataGrid x:Name="orderOfBattleDataGrid" Grid.Column="2"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding orderOfBattle}"
                      Background="DarkGray"
                      SelectedValuePath="id"
                      FrozenColumnCount="2"
                      SelectionMode="Extended"
                      SelectionUnit="CellOrRowHeader"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      PreviewKeyDown="oobdg_PreviewKeyDown"
                      CellEditEnding="dataGrid_CellEditEnding"
                      DataGrid.CellStyle="{StaticResource styleUnitCell}" Margin="0,0,5,0">

                <DataGrid.Resources>
                    <DataTemplate x:Key="oWeaponTemplate">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                            <Button Name="WeaponButton" Click="oobdg_WeaponButtonClick" Content="::"/>
                            <TextBox Text="{Binding weapon, Mode=OneWay}" IsReadOnly="True"/>
                        </StackPanel>
                    </DataTemplate>
                    <DataTemplate x:Key="oFlag1Template">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                            <Button Name="FlagButton" Click="oobdg_Flag1ButtonClick" Content="::"/>
                            <TextBox Text="{Binding flag, Mode=OneWay}" IsReadOnly="True"/>
                        </StackPanel>
                    </DataTemplate>
                    <DataTemplate x:Key="oFlag2Template">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                            <Button Name="FlagButton" Click="oobdg_Flag2ButtonClick" Content="::"/>
                            <TextBox Text="{Binding flag2, Mode=OneWay}" IsReadOnly="True"/>
                        </StackPanel>
                    </DataTemplate>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Rank" SortMemberPath="echelon.id" IsReadOnly="True" CellTemplate="{StaticResource sRankTemplate}"/>
                    <DataGridTextColumn Header="Id"       Binding="{Binding id}"    CellStyle="{StaticResource styleId}"/>
                    <DataGridTextColumn Header="Name 1"   Binding="{Binding name1}"/>
                    <DataGridTextColumn Header="Name 2"   Binding="{Binding name2}"/>
                    <DataGridComboBoxColumn Header="Unit Class" Width="100" x:Name="unitClass"
                                           SelectedValueBinding="{Binding unitClass, Mode=TwoWay}"
                                           DisplayMemberPath="{Binding unitClass}"/>
                    <DataGridTextColumn Header="OOB Config" Binding="{Binding oobconfig}"/>
                    <DataGridTextColumn Header="Portrait"   Binding="{Binding portrait}"/>
                    <DataGridTemplateColumn Header="Weapon"    CellTemplate="{StaticResource oWeaponTemplate}"/>
                    <DataGridTemplateColumn Header="Flag 1"    CellTemplate="{StaticResource oFlag1Template}"/>
                    <DataGridTemplateColumn Header="Flag 2"    CellTemplate="{StaticResource oFlag2Template}"/>
                    <DataGridTextColumn Header="Head Count"    Binding="{Binding headCount}"/>
                    <DataGridTextColumn Header="Ammo"          Binding="{Binding ammo}"/>
                    <DataGridTemplateColumn Header="Formation" CellTemplate="{StaticResource FormationTemplate}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
